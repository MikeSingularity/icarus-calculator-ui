---
description: Read upon initializing environment, updated automatically upon task completion (/ready).
---

# Current Mission

NONE / AWAITING INSTRUCTION

# Active Task

NONE

# Session Log (2026-04-06)

- **UI/UX Polish:** Centered search overlay, refined React Flow control visibility (dark theme), and removed branding attribution.
- **Quantity Formatting Implementation:** Added `display_operations` and `unit` support to `RecipeNode.tsx` via new `quantityFormatter.ts` utility.
- **State Lifecycle & GC:** Implemented reachability-based garbage collection in `usePlanStore.ts` to automatically purge stale node data.
- **ADR-002 Implemented:** Refactored Production Graph to a Unified DAG with a Dynamic Shopping List. Removed `MaterialPane` and `DynamicLinker`.
- **Software Design Document (SDD) Completed:** Built [docs/software-design-document.md](docs/software-design-document.md) based on UX overview and user feedback.

# Known Project Gotchas

1. **Toolchain Mapping:** This project adheres strictly to `pnpm`, `eslint`, `prettier`, and `vitest`.
2. **Standard Scripts:** Only run tasks through the `scripts/*.sh` lifecycle files (e.g., `./scripts/test.sh`, `./scripts/lint.sh`).
3. **Architecture Rules:** Follow `~/gemini/global-rules.md` strictly. No generic identifiers.
4. **Docs vs Code:** Maintain sync between `README.md` / `docs/architecture-decisions` and source files.
5. **Local Configs:** `.tsconfig.json` and `eslint.config.js` are managed locally to support the custom Vite 8 / ESM environment.
